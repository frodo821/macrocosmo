[package]
name = "macrocosmo"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.18.1"
bevy_egui = "0.39.1"
mlua = { version = "0.11", features = ["luajit", "vendored", "send"] }
rand = "0.9"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
